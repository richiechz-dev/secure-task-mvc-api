[project]
name = "securetask-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pwdlib[argon2]>=0.3.0",
    "sqlalchemy>=2.0.46",
]
